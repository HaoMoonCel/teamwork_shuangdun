# AI 真算法服务地址（M2 联调用）
#
# 使用方法：
#   1. 复制本文件为 .env.local（已被 .gitignore 忽略，不会入库）
#   2. 填入杨的服务器公网地址，例如：
#      VITE_AI_API_URL=http://1.2.3.4:5000
#   3. 重启 dev server（npm run dev）后页面自动走真算法
#
# 留空 / 不配置 → 页面走模拟数据（M1 演示版），不影响日常演示
VITE_AI_API_URL=
